# Local output from scripts/seed-register-users.sh (never commit)
seed-users-report.csv

# TestFlight credentials (never commit)
scripts/testflight-credentials.json

# GitHub token for /push (never commit)
scripts/github-token

# Pinnacle Flutter app — separate repo (github.com/VoltisLab/Pinnacle)
pinnacle/

# Build
build/
DerivedData/
*.xcarchive

# Xcode
*.pbxuser
*.mode1v3
*.mode2v3
xcuserdata/

# macOS
.DS_Store

# API keys (paste your key in Secrets.plist; do not commit)
Prelura-swift/Secrets.plist

# Firebase iOS config (per-environment; create a dedicated Swift Firebase project — see docs/FIREBASE_IOS_SETUP.md)
Prelura-swift/GoogleService-Info.plist

# Apple APNs auth keys (.p8) — never commit; upload in Firebase Console only (see docs/FIREBASE_IOS_SETUP.md)
*.p8
