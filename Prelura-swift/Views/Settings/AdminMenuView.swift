import SwiftUI

/// Staff-only entry from Settings / Menu. Presents the full admin dashboard (orders, issues, reports, bulk actions).
struct AdminMenuView: View {
    var body: some View {
        AdminDashboardView()
    }
}
